version: "3.1"

rules:
  - rule: rule_value
    steps:
      - intent: intent_value
      - action: tailor_response

  - rule: start
    steps:
      - intent: greet
      - action: action_vehicle_body
