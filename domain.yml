version: "3.1"

intents:
  - order_pizza
  - provide_address

entities:
  - pizza_type
  - address

slots:
  pizza_type:
    type: text
    mappings:
      - type: from_entity
        entity: pizza_type
  address:
    type: text
    mappings:
      - type: from_entity
        entity: address

actions:
  - action_ask_address
  - action_confirm_order
